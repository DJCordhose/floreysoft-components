{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.10.7"
  },
  "scripts": {
    "publish" : "lerna run tsc && lerna publish"
  }
}
